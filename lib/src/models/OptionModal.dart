import 'package:flutter/material.dart';

import '../utils/constant.dart';

class Option {
  Icon icon;
  String title;
  String subtitle;
  bool isChecked;

  Option({this.icon, this.title, this.subtitle,this.isChecked});

}

